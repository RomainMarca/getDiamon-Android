<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.wildcodeschool.getdiamond">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".BuildJewelryActivity" />
        <activity android:name=".LoadingActivity" />
        <activity android:name=".AskListActivity" />
        <activity android:name=".ExchangeCreateActivity" />
        <activity android:name=".ShowExchangeActivity" />
        <activity android:name=".RankingActivity" />
        <activity android:name=".MyExchangeActivity" />
        <activity android:name=".ExchangeWithmeActivity"></activity>
    </application>

</manifest>